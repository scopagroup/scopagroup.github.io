source "https://rubygems.org"

# Pin to the GitHub Pages gem so you get the exact plugin versions GitHub uses.
gem "github-pages", group: :jekyll_plugins

# Optional: speed up file watching on Windows
gem "wdm", ">= 0.1.0", platforms: [:mingw, :x64_mingw, :mswin]

# Optional: if you ever add SCSS/SASS locally
gem "sass-embedded", "~> 1.79", platform: [:ruby]
